# pb_pb2_new_page Environment Variables

# ======================================
# Google Sheets API Configuration
# ======================================

# Google Sheets ID (required)
# 스프레드시트 URL에서 추출: https://docs.google.com/spreadsheets/d/{SHEET_ID}/edit
# Example: 1ipkHdYdQhIAfUBkNUWHkFqcgP0aOXLVO14MYXWscEPk
GOOGLE_SHEET_ID=your_google_sheet_id_here

# Service Account JSON file path (required)
# 상대 경로 또는 절대 경로 모두 가능
# Example: credentials/service-account.json
GOOGLE_SERVICE_ACCOUNT_FILE=credentials/service-account.json

# ======================================
# Flask Server Configuration (Optional)
# ======================================

# Flask server port (default: 5001)
# Port 5001이 이미 사용 중이면 다른 포트로 변경
# FLASK_PORT=5001

# Flask debug mode (default: False)
# 개발 중에는 True로 설정하여 자동 재시작 및 디버깅 활성화
# FLASK_DEBUG=False

# Flask host (default: 127.0.0.1)
# 외부 접근 허용 시 0.0.0.0으로 변경 (보안 주의)
# FLASK_HOST=127.0.0.1

# ======================================
# Output Configuration (Optional)
# ======================================

# Output directory for generated HTML files (default: output/)
# OUTPUT_DIR=output/

# Date format for folder structure (default: %Y%m%d)
# OUTPUT_DATE_FORMAT=%Y%m%d

# ======================================
# Image Processing (Optional)
# ======================================

# Image cache directory (default: output/assets/images/)
# IMAGE_CACHE_DIR=output/assets/images/

# Image download timeout in seconds (default: 30)
# IMAGE_DOWNLOAD_TIMEOUT=30

# Maximum image size in MB (default: 10)
# MAX_IMAGE_SIZE_MB=10

# ======================================
# Logging Configuration (Optional)
# ======================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
# LOG_LEVEL=INFO

# Log file path (default: None - console only)
# LOG_FILE=logs/pb_pb2.log

# ======================================
# Notes
# ======================================

# 1. 이 파일을 .env로 복사하여 사용하세요:
#    cp .env.example .env
#
# 2. .env 파일은 Git에 커밋하지 마세요 (.gitignore에 이미 포함됨)
#
# 3. 필수 환경 변수만 설정하면 나머지는 기본값이 사용됩니다
#
# 4. Google Sheets ID 찾기:
#    - Google Sheets URL 열기
#    - URL 형식: https://docs.google.com/spreadsheets/d/{SHEET_ID}/edit
#    - {SHEET_ID} 부분을 복사하여 GOOGLE_SHEET_ID에 입력
#
# 5. 서비스 계정 설정:
#    - docs/SETUP_GUIDE.md 참조
#    - Google Cloud Console에서 서비스 계정 키 다운로드
#    - credentials/service-account.json에 저장
#    - Google Sheets와 서비스 계정 공유 필수
#
# 6. 환경 변수 로드 확인:
#    python3 -c "import os; from dotenv import load_dotenv; load_dotenv(); print(os.getenv('GOOGLE_SHEET_ID'))"
